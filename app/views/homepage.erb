<h2>Tweets</h2>
<% @tweets.each do |tweet| %>
<div class="tweet">
  <p> <a href="/<%= User.find(tweet.user_id).username %>"><%= User.find(tweet.user_id).username %></a></p>
  <p><%= tweet.text %></p>
  <p><%= tweet.created_at.to_date %></p>
</div>
<% end %>